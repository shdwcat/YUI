/// @description



// Inherit the parent event
event_inherited();

// gotta update this every frame since it could be animated
opacity = opacity_value.value * parent.opacity

// also need this
// todo define value on root types...
xoffset = xoffset_value.value * parent.xoffset
